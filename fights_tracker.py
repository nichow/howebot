#fights_tracker.py

import numpy as np
from datetime import datetime.utcnow as utcnow

class fights_tracker(self, game):
  self.game = game
  self.month = utcnow.month
  self.ratings = []
  self.avg = np.mean(np.array(ratings))
